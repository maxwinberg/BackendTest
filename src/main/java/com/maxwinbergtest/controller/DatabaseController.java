package com.maxwinbergtest.controller;

public class DatabaseController{

/**
 * I didnt use this class, but if i had more views here i would use a controller layer as a abstract layer to communicate with the model from the view
 * This would add some logic in how the view could integrate with the model and have control over how the model components are used.
 */

}